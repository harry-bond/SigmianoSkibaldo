<!DOCTYPE html>
<html lang="en">
  <!-- Head Contains all the metadata -->
  <head>
    <meta charset="utf-8">
    <title>Sigmiano Skibaldo</title>
    <meta name="author" content="Harry Bond">
    <meta name="viewport" content ="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/general-styles.css">
  </head>

  <body>
    <header>
      <h1>Sigmiano Skibaldo</h1>
      <div id="horizontal-flex">
        <a style="background-color: white;" href="/home"><h2>The<br>League</h2></a>
        <a href="/player-statistics"><h2>Player<br>Statistics</h2></a>
        <a href="/our-team"><h2>Our<br>Team</h2></a>
        <a href="/as-it-stands-awards"><h2>As it Stands<br>Awards</h2></a>
      </div>
    </header>

    <main>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>P</th>
            <th>W</th>
            <th>D</th>
            <th>L</th>
            <th>GF</th>
            <th>GA</th>
            <th>GD</th>
            <th>PTS</th>
          </tr>
        </thead>
        <tbody>
          <% @teams.each do |team| %>
            <tr>
              <% if team.team_name == "Sigmiano Skibaldo\t" %>
                <th style="font-weight: bold; font-size: 1.8vw;"><%= h team.team_name%></th>
                <td style="font-weight: bold;"><%= h team.played %></td>
                <td style="font-weight: bold;"><%= h team.wins %></td>
                <td style="font-weight: bold;"><%= h team.drawn %></td>
                <td style="font-weight: bold;"><%= h team.lost %></td>
                <td style="font-weight: bold;"><%= h team.goals_for %></td>
                <td style="font-weight: bold;"><%= h team.goals_against %></td>
                <td style="font-weight: bold;"><%= h team.goal_difference %></td>
                <td style="font-weight: bold;"><%= h team.points %></td>
              <% else %>
                <th><%= h team.team_name%></th>
                <td><%= h team.played %></td>
                <td><%= h team.wins %></td>
                <td><%= h team.drawn %></td>
                <td><%= h team.lost %></td>
                <td><%= h team.goals_for %></td>
                <td><%= h team.goals_against %></td>
                <td><%= h team.goal_difference %></td>
                <td><%= h team.points %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </main>

    <footer>
      <h6>©Harry Bond</h6>
      <h6>2024</h6>
    </footer>
  </body>
</html>